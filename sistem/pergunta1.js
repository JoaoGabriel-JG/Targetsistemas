let indice = 13, soma = 0, K = 0    // Variáveis em let para a soma e o K o indice poderia ser const

while (K < indice) {    // Estrutura while que seria o enquanto
    K = K + 1
    
    soma = soma + K
}

console.log(soma)